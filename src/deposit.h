#ifndef DEPOSIT_H
#define DEPOSIT_H

#include <iostream>
#include <cmath>
#include <stdio.h>
#include <cmath>

using std::cout;
using std::endl;

int zero(int &d, int ye);
int one(int &d, int ye);
int two(int &d, int ye);
int three(int &d, int ye);

#endif
